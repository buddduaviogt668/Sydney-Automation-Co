SHAC Studio — explicit-size SVG export for 5500AC2

These SVGs include explicit width and height attributes as well as viewBox values. If the 5500AC2 still shows a blank image, use the matching PNG from ../transparent_png or ../display_png; the PNG exports do not require the controller to render SVG text, CSS, filters, or vector metadata.
